<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MyPdfViewer.Pages.AllPdfsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:MyPdfViewer.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon
                    FontSize="28"
                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                    Glyph="&#xE71D;" />
                <TextBlock
                    Style="{ThemeResource TitleTextBlockStyle}"
                    Text="All PDFs"
                    VerticalAlignment="Center" />
            </StackPanel>
            <Button
                x:Name="BtnAddPdf"
                Grid.Column="1"
                Click="BtnAddPdf_Click"
                Content="Open PDF"
                Style="{ThemeResource AccentButtonStyle}"
                VerticalAlignment="Center">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="O" Modifiers="Control" />
                </Button.KeyboardAccelerators>
            </Button>
        </Grid>

        <!--  File list with per-item Add-to-Folder action  -->
        <ListView
            x:Name="PdfListView"
            Grid.Row="1"
            IsItemClickEnabled="True"
            ItemClick="PdfListView_ItemClick"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4,10" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <FontIcon
                            Grid.Column="0"
                            FontSize="24"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Glyph="&#xEA90;"
                            VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                            <TextBlock
                                FontSize="13"
                                FontWeight="SemiBold"
                                Text="{Binding FileName}"
                                TextTrimming="CharacterEllipsis" />
                            <TextBlock
                                FontSize="11"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Text="{Binding LastOpenedDisplay}"
                                TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <!--  "Add to Folder" per-item button  -->
                        <Button
                            Grid.Column="2"
                            Click="AddToFolder_Click"
                            Tag="{Binding FileId}"
                            ToolTipService.ToolTip="Add to Folder"
                            VerticalAlignment="Center">
                            <FontIcon FontSize="13" Glyph="&#xE8B7;" />
                        </Button>

                        <FontIcon
                            Grid.Column="3"
                            FontSize="14"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Glyph="&#xE76C;"
                            VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
